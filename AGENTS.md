# ContextSystem — エージェント向け入口

**仕様の本文（正）**は次のファイルにすべて書いてある。**必ずそちらを読むこと**。

→ **`CONTEXTSYSTEM_SPEC.md`**（リポジトリルート）

この `AGENTS.md` には本文を置かない（ツールがここだけを開く場合の誘導専用）。  
コンテキストに載せるなら、**直接 `CONTEXTSYSTEM_SPEC.md` を添付・参照**してもよい。
